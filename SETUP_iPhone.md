# 毎朝 iPhone で見られるようにする手順

GitHub の無料枠だけで完結します。Mac の電源は入っていなくて構いません。所要 15 分ほど。

```
毎朝 07:00 JST
  GitHub Actions が起動
    → yfinance で日米ETFの価格を取得
    → 予測を計算
    → docs/index.html を更新してコミット
  GitHub Pages が自動で公開
    → iPhone のホーム画面のアイコンから開く
```

---

## 1. GitHub にリポジトリを作る

1. https://github.com/new を開く
2. Repository name: `jp-us-leadlag`（任意）
3. **Public** を選ぶ
   - Private でも動きますが、Actions の無料枠が月 2,000 分に制限され、Pages も有料プランが必要です。Public なら両方とも無料・無制限です
   - 中身は公開されている論文の再現コードと予測結果だけです。公開したくない場合は後述の「Private にしたい場合」を読んでください
4. README などは追加せず、空のまま **Create repository**

## 2. コードを push する

Mac のターミナルで、`jp_us_leadlag` フォルダの中に入って:

```bash
cd ~/path/to/jp_us_leadlag     # 展開した場所に合わせてください

git init
git add .
git commit -m "初回コミット"
git branch -M main
git remote add origin https://github.com/<あなたのユーザー名>/jp-us-leadlag.git
git push -u origin main
```

## 3. Actions に書き込み権限を与える

毎朝の更新結果をリポジトリにコミットするために必要です。

1. リポジトリの **Settings** → 左メニュー **Actions** → **General**
2. 一番下の **Workflow permissions**
3. **Read and write permissions** を選んで **Save**

## 4. 初回を手動で実行する

1. リポジトリの **Actions** タブを開く
2. 左のリストから **毎朝の予測ページを更新** を選ぶ
3. 右側の **Run workflow** → 緑の **Run workflow** ボタン
4. 1〜3 分で完了します。緑のチェックが付けば成功

> 失敗した場合は、そのログを開いて赤い行を見てください。よくあるのは手順 3 の権限設定のし忘れ（`Permission denied`）と、Yahoo Finance 側の一時的な混雑（`価格データを取得できませんでした`）です。後者はもう一度 Run workflow すれば通ることがほとんどです。

## 5. GitHub Pages を有効にする

1. **Settings** → 左メニュー **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `main` / フォルダは **`/docs`** を選んで **Save**
4. 1〜2 分待つと、同じ画面の上部に URL が出ます

```
https://<あなたのユーザー名>.github.io/jp-us-leadlag/
```

## 6. iPhone のホーム画面に置く

1. iPhone の **Safari** で上の URL を開く（Chrome ではなく Safari である必要があります）
2. 下部中央の **共有ボタン**（□に↑）をタップ
3. 少し下にスクロールして **「ホーム画面に追加」**
4. 名前を「日米リードラグ」などにして **追加**

これでホーム画面にアイコンができます。タップするとアドレスバーのないアプリのような画面で開きます。ダークモードにも自動で追従します。

---

## 見方

| 表示 | 意味 |
|---|---|
| **日中の地合い** | 日本株のリターンはほぼ夜間に発生し、日中（寄付き→大引け）は恒常的にマイナスです。上下を当てる表示ではなく、その**下押し圧力が過去250営業日と比べて強いか弱いか**を五分位で示しています。「弱い」は上昇に転じるという意味ではありません |
| **ロング / ショート** | 業種間の相対的な強弱。上位5業種を買い、下位5業種を売るのが論文の戦略です |
| **業種をタップ** | その業種ETFの構成銘柄が出ます。組入比率の大きい順に上位10銘柄、さらにタップすると全銘柄。出所は野村アセットマネジメントの「組入全銘柄情報」（月次更新）です |
| **「弱」バッジ** | シグナルが最大値の 2 割未満で、実質ノイズです。λ=0.9 では強いシグナルが 8 業種に集中するため、5番目の枠はしばしばこうなります |
| **直近の実績** | このページが過去に出した予想を、その日の実現リターンで後から採点したもの。取引コストは含みません。実データ検証では①片道4.6bpsで損益が消える回転率、②2026年の大幅な悪化、が確認されています |
| **赤い警告帯** | データが 4 日以上古いときに出ます。自動更新が止まっているサインです |

---

## 運用上の注意

**更新のタイミング** — GitHub の cron は混雑時に 5〜20 分遅れることがあります。東京の寄付き（9:00 JST）までには余裕がありますが、07:00 ちょうどに必ず出るわけではありません。

**サマータイム** — ワークフローは UTC 固定なので、米国がサマータイムの期間（3月〜11月）は NY 引けから更新までの間隔が 1 時間長くなります。予測内容には影響しません。

**祝日** — 日本の祝日は判定していません。東京が休場の日は、そのページの予想がそのまま翌立会日に持ち越されるのではなく、次の朝に新しい予想へ置き換わります。休場日の朝に開いたページは無視してください。

**60 日で自動停止** — GitHub は 60 日間リポジトリに何のアクティビティもないと、スケジュール実行を自動で止めます。毎日コミットが入るのでこの構成では止まりませんが、もし更新が途絶えたら Actions タブから手動で Run workflow すれば再開します。

**履歴** — `docs/history.json` に過去の予想と採点が溜まっていきます。これが実績表示の元データなので、削除しないでください。

---

## Private にしたい場合

Private リポジトリでは GitHub Pages に有料プラン（GitHub Pro など）が必要です。無料のまま人目に触れさせたくない場合は、次のどちらかになります。

- リポジトリは Private にして、Pages は諦め、Actions で生成した HTML を毎朝メールで自分に送る
- 手元の Mac で実行して iCloud Drive に保存する（`python scripts/build_page.py --outdir ~/Library/Mobile\ Documents/com~apple~CloudDocs/leadlag` を launchd で毎朝叩く）

必要であればどちらでも作ります。

---

## 設定を変えたいとき

`.github/workflows/daily.yml` の該当行を編集して push してください。

```yaml
- cron: "0 22 * * 0-4"        # 実行時刻。22:00 UTC = 07:00 JST
```

```yaml
run: python scripts/build_page.py --outdir docs --lam 0.9 --window 60
```

`--lam`（正則化の強さ）と `--window`（推定期間）が結果に一番効きます。変える前に必ず手元で `python -m leadlag.cli sweep` を実行して、その水準がバックテストでどうなるか確かめてください。
